import 'package:flutter/material.dart';

class RequestTricycleSreen extends StatelessWidget {
  static const screenName = '/requesttricycle';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('request'),
    );
  }
}
