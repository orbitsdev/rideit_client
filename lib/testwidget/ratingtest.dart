import 'package:flutter/material.dart';

class Ratingtest extends StatelessWidget {
const Ratingtest({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container(
      child: Column(
        children: [
          
        ],
      ),
    );
  }
}